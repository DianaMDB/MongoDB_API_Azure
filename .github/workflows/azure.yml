name: Build and deploy Python Function App

on:
  push:
    branches:
      - main  # or your actual branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Azure Functions Core Tools
        run: |
          curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
          npm install -g azure-functions-core-tools@4 --unsafe-perm true

      - name: Write publish profile to file
        run: |
          echo "${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}" > publishProfile.publishsettings

      - name: Publish to Azure Function App
        run: |
          func azure functionapp publish mdb-dataapi-repl --python --publish-profile publishProfile.publishsettings
